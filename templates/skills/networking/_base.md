# 네트워킹 규칙

API 호출, 데이터 페칭, 원격 통신과 관련된 작업에 이 스킬을 적용한다.

## 핵심 원칙

- 모든 네트워크 접근은 {{NETWORK_LAYER_NAME}}을 통해야 한다.
- UI 컴포넌트에서 원격 API를 직접 호출하지 않는다.
- 응답은 적절한 레이어에서 파싱하고 매핑한다.
- 타임아웃, 취소, 디코딩 실패, 서버 에러 경로를 처리한다.
- 토큰, API 키, 민감한 요청/응답 본문을 절대 로그에 남기지 않는다.
- 적용 가능한 경우 멱등성 있는 재시도를 선호한다.
