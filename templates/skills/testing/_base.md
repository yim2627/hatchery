# 테스트 규칙

로직 변경이 도입될 때 이 스킬을 적용한다.

## 핵심 원칙

- 비자명한(non-trivial) 새 로직에는 테스트가 필요하다.
- 버그 수정 시 가능하면 회귀 테스트를 포함한다.
- 구현 세부사항이 아닌 동작을 검증하는 테스트를 작성한다.
- 결정적(deterministic) 테스트를 선호한다.
- 필요한 경우가 아니면 타이밍에 의존하는 불안정한 테스트를 피한다.
- 외부 의존성은 mock이나 fake로 대체한다.
- 프레젠테이션 레이어 로직의 상태 전이를 테스트한다.
