# 로깅 규칙 — Web

- 서버에서는 구조화된 로깅(Pino, Winston, 또는 일관된 형식의 console)을 사용한다.
- 프로덕션 클라이언트 코드에서 `console.log`를 피한다 — 확립된 에러 트래킹(Sentry 등)을 사용한다.
- 서버 사이드에서 요청 컨텍스트(request ID, user ID)를 로그에 포함한다.
- 클라이언트 사이드 코드에서 크레덴셜, 토큰, PII를 절대 로그에 남기지 않는다.
