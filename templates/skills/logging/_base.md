# 로깅 규칙

진단, 관찰 가능성, 에러 리포팅과 관련된 작업에 이 스킬을 적용한다.

## 핵심 원칙

- 개발자 진단에는 {{LOGGING_SYSTEM}}을 사용한다.
- 목적 없는 노이즈 로그를 추가하지 않는다.
- 지원되는 경우 구조화된 로그를 선호한다.
- 시크릿, 토큰, 원본 개인정보, 보호 의료 데이터를 절대 로그에 남기지 않는다.
- 적절한 로그 레벨을 사용한다: error(실패), warning(저하 상태), info(핵심 이벤트), debug(개발용).
