# 동시성 규칙

비동기 작업, 태스크 오케스트레이션, 취소, 스레드 안전성과 관련된 작업에 이 스킬을 적용한다.

## 핵심 원칙

- fire-and-forget 패턴보다 구조적 동시성을 선호한다.
- UI에 노출되는 상태 변경은 UI 스레드/actor에서 수행해야 한다.
- 유저가 트리거한 비동기 작업에는 취소를 지원한다.
- 경쟁 조건이 발생할 수 있는 공유 가변 상태를 피한다.
- 재진입(re-entrancy)과 반복 호출 동작을 고려한다.
- 비동기 상태 변경은 예측 가능한 전이를 우선한다.
